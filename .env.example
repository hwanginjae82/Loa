# 로스트아크 Open API에서 발급한 JWT만 .env.local에 넣으세요.
# Supabase Dev DB 설정은 Git에 포함된 .env.development를 사용합니다.
# 운영 DB 설정은 배포 환경 또는 .env.production.local에서 관리합니다.
# bearer 접두사는 있어도 되고 없어도 됩니다.
LOSTARK_API_JWT=
